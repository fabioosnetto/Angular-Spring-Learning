import { NumMarkerPipe } from './num-marker.pipe';

describe('NumMarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new NumMarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
