import { TextBasicStyleDirective } from './text-basic-style.directive';

describe('TextBasicStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBasicStyleDirective();
    expect(directive).toBeTruthy();
  });
});
