import { TitleBasicStyle } from './title-basic-style';

describe('TitleBasicStyle', () => {
  it('should create an instance', () => {
    const directive = new TitleBasicStyle();
    expect(directive).toBeTruthy();
  });
});
